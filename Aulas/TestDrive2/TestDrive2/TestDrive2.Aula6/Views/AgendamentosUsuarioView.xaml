<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:TestDrive.Converters"
             x:Class="TestDrive.Views.AgendamentosUsuarioView"
             Title="Agendamentos Realizados">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:AgendamentoConfirmadoConverter x:Key="agendamentoConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout Margin="25">
        <ListView ItemsSource="{Binding Lista}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding Modelo}" FontSize="20" 
                                       VerticalTextAlignment="Center"
                                        TextColor="{Binding Confirmado, Converter={StaticResource Key=agendamentoConverter}}"></Label>
                                <Label Text=" - " VerticalTextAlignment="Center"
                                        TextColor="{Binding Confirmado, Converter={StaticResource Key=agendamentoConverter}}"></Label>
                                <Label Text="{Binding PrecoFormatado}" FontAttributes="Bold" 
                                       VerticalTextAlignment="Center"
                                        TextColor="{Binding Confirmado, Converter={StaticResource Key=agendamentoConverter}}"></Label>
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>